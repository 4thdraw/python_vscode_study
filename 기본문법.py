# %% [문법-1] 변수 만들기
이름 = "송파 요양원"
개수 = 10
시설목록 = ["요양원", "병원", "복지관"] # 리스트

print(이름, 개수)
print(시설목록[0]) # 첫 번째 아이템 꺼내기

# %% [문법-2] 반복문과 조건문
for 시설 in 시설목록:
    if 시설 == "요양원":
        print(시설 + "은(는) 집중 관리 대상입니다.")
    else:
        print(시설 + "은(는) 일반 시설입니다.")

# %% [문법-3] 나만의 도구 만들기
def 인사하기(시설이름):
    return 시설이름 + "에 오신 것을 환영합니다!"

메시지 = 인사하기("송파복지관")
print(메시지)


# %% [판다스-1] 직접 표 만들기
import pandas as pd

# 우리가 직접 만드는 데이터 (딕셔너리 형태)
데이터 = {
    "년월": ["2023-01", "2023-02", "2023-03", "2023-04"],
    "신고수": [5, 8, 3, 10],
    "폐업수": [1, 0, 2, 1]
}

# 판다스 표(DataFrame)로 변신!
df = pd.DataFrame(데이터)

# Variables에서 df를 클릭해 확인해보세요!


# %% [판다스-2] 데이터 가공
# 새로운 열 추가 (합계 계산)
df['총변동'] = df['신고수'] + df['폐업수']

# 아까 배웠던 뒤집기(T) 복습! 
# (인덱스를 '년월'로 바꾸고 뒤집으면 더 깔끔합니다)
df_flipped = df.set_index('년월').T

# Variables에서 바뀐 모양을 확인하세요.
# %%
# 에러가 나는 케이스 (수강생들이 정말 많이 틀려요!)
# 데이터 = {
#     "이름": ["A", "B", "C"], # 3개
#     "나이": [20, 30]          # 2개 (에러 발생!)
# }